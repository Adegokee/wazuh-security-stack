# 1. Generate SSL certificates
chmod +x generate-ssl-certs.sh && ./generate-ssl-certs.sh

# 2. Generate Wazuh cluster key
openssl rand -hex 32 > config/wazuh_cluster.key

# 3. Update AWS credentials
vi aws_credentials/credentials

# 4. Start everything
docker-compose up -d





# Start all services
docker-compose up -d

# Check status
docker-compose ps

# View logs
docker-compose logs -f




# Check all containers are running
docker-compose ps

# Test Wazuh Dashboard
curl -k https://localhost

# Test Elasticsearch
curl -k -u admin:SecretPassword https://localhost:9200

# View real-time logs
docker-compose logs -f nginx-proxy